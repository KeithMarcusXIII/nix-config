{
  config,
  lib,
  pkgs,
  ...
}: {
  # OMLX_API_KEY is now injected via shell auto-load from /run/secrets/
  # See programs.zsh.envExtra in flake-modules/cli-tools/homeManagerModules/default.nix
}
